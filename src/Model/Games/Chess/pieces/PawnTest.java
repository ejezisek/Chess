package Model.Games.Chess.pieces;

import static org.junit.Assert.*;

import org.junit.Test;

public class PawnTest {

	/**
	 * Creates a new chess board and checks to ensure that pawns can move upwards.  Then checks to ensure that pawns can attack.
	 * Afterwards checks for en passant and ensures that the pawn can't attack non pawns during en passant.
	 */
	@Test
	public void test() {
		//Check to see that getting the 
	}

}
